{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run",
            "type": "shell",
            "command": "go build && ./channelpack.exe",
            "group": {
                "isDefault": true,
                "kind": "build"
            }
        }
    ]
}